import Backdrop from "@/components/commons/backdrop";

const playground = () => {
  const data = {
    title: "Playground",
  };
  return <Backdrop pageHeaderData={data}>Playground</Backdrop>;
};

export default playground;
